// AsyncStorage keys
export const STORAGE_KEYS = {
  USER_TOKEN: '@stem_user_token',
  USER_DATA: '@stem_user_data',
  NEWS_CACHE: '@stem_news_cache',
  BOOKS_CACHE: '@stem_books_cache',
  BOOKMARKS: '@stem_bookmarks',
} as const;